# JobPulse Mobile App

Phone-first contractor app (Nuxt). MVP is used in the mobile browser / as a PWA.

```bash
npm install
npm run dev -- --port 3000
```

API base URL: `NUXT_PUBLIC_API_BASE_URL` (default `http://localhost:8000`).
