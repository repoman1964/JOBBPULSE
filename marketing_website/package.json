{
  "name": "jobbpulse-marketing",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "NITRO_PRESET=static nuxt generate && rm -rf .wrangler/deploy",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "nuxt": "^4.5.0",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
