.PHONY: help

help:
	@echo "Contractor UI only. The JobbPulse API lives in ../api/"
	@echo "  From repo root: make infra-up && make api-dev"
	@echo "  Then:           cd frontend && NUXT_PUBLIC_API_MODE=http npm run dev"
