{
  "name": "red-clay-website",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run",
    "deploy": "NUXT_PUBLIC_API_BASE_URL=https://api.jobbpulse.com npm run build && wrangler pages deploy dist --project-name=red-clay-website"
  },
  "dependencies": {
    "nuxt": "^4.5.0",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "vitest": "^3.2.7",
    "wrangler": "^4.72.0"
  }
}
