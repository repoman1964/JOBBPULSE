# JobPulse Directory

Public local directory (Nuxt SSR) for contractor profiles and project listings.

```bash
npm install
npm run dev -- --port 3001
```

API base URL: `NUXT_PUBLIC_API_BASE_URL` (default `http://localhost:8000`).
